name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production   # <-- THIS enables protection
    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Deploying to production"
